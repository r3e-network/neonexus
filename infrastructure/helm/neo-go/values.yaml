replicaCount: 1

image:
  repository: nspccdev/neo-go
  pullPolicy: IfNotPresent
  tag: "0.106.0"

network: "mainnet" # mainnet or testnet
syncMode: "full" # full or archive

node:
  maxPeers: 100
  p2pPort: 10333
  rpcPort: 10332
  prometheusPort: 2112

resources:
  requests:
    cpu: "2"
    memory: "4Gi"
  limits:
    cpu: "4"
    memory: "8Gi"

persistence:
  enabled: true
  size: 200Gi # Increase to 2Ti for archive
  storageClass: "hcloud-volumes" # Use do-block-storage when running on DigitalOcean

snapshot:
  enabled: true
  url: "https://sync.ngd.network/mainnet/chain.acc.tar.gz" # Example official snapshot URL

service:
  type: ClusterIP
  annotations: {}
